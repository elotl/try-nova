{
  "CN": "kube-apiserver",
  "hosts": [
    "${APISERVER_PUBLIC_ENDPOINT}",
    "*.${NAMESPACE}.svc.cluster.local",
    "*.${NAMESPACE}.svc",
    "localhost",
    "${EXTRA_IP}"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  }
}
